define([
    'angular'
], function (
    angular
) {
    'use strict';

    // Declare app level module which depends on filters, and services
    angular.module('porscheEC.config', [])
        .constant("DEBUG", true);
});